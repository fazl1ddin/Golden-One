---
category: Actions
---

# ThemeButton

Icon button that flips the light/dark token set. Lives in the topbar's right slot next to `LangSwitch`.

```tsx
<ThemeButton onClick={() => setDark(v => !v)} />
```
