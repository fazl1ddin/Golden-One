---
category: Actions
---

# LangSwitch

Segmented language selector. The product ships Russian and Uzbek; the switch changes operator-facing copy.

```tsx
<LangSwitch value={lang} onChange={setLang} />
```

Defaults to `["ru","uz"]`; pass `options` for a different set.
