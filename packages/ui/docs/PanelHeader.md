---
category: Layout
---

# PanelHeader

A panel's title bar. `title` is set in small uppercase; `count` appends a muted total; `action` takes a link or control on the right.

```tsx
<PanelHeader title="Устройства" count={128} action={<button className="go-link">Все →</button>} />
```
