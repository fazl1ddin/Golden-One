---
category: Forms
---

# Textarea

Multi-line field, vertically resizable. Its main use is the Lost Mode message the customer will read on the locked phone — pair it with `LostModePreview` so the operator sees the result while typing.

```tsx
<Textarea defaultValue="Устройство заблокировано в связи с просрочкой платежа." />
```
